<% provide(:title, 'All users') %>
<h1>All users</h1>
<% if current_user.admin? %>
	<%= link_to "Add User", signup_path, class: "btn btn-large btn-primary" %>
<% end %>
<%= will_paginate %>

<ul class="users">
  <%= render @users %>
</ul>

<%= will_paginate %>
